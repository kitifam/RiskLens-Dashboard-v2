# 🔔 ตั้งค่าส่ง Line & Email (แบบง่าย อ่านแล้วทำตามได้)

แอป RiskLens ใช้ **Backend** (โฟลเดอร์ `api/`) เป็นตัวส่ง Line และอีเมลจริง  
คีย์และ Token **ไม่เก็บในโค้ด** แล้ว เก็บใน **Environment Variables** เท่านั้น

---

## 📍 ต้องตั้งค่าที่ไหนบ้าง (สรุปสั้นๆ)

| ที่ต้องตั้ง | ใช้เมื่อไหร่ |
|------------|----------------|
| **Vercel (บนเว็บ)** | ตอนเปิดแอปที่ deploy บน Vercel (เช่น ลิงก์จาก GitHub) |
| **.env.local (ในเครื่อง)** | ตอนรันทดสอบบนเครื่องตัวเอง ให้ส่ง Line/Email ได้ |

---

## 1️⃣ ตั้งค่าบน Vercel (ให้ส่ง Line & Email ได้ตอนเปิดจากเว็บ)

1. เปิด **Vercel** → เข้าโปรเจกต์ RiskLens ของคุณ  
   (ถ้ายังไม่มี ให้เชื่อม GitHub repo ก่อนแล้ว deploy)

2. ไปที่ **Settings** (ของโปรเจกต์) → **Environment Variables**

3. กด **Add New** แล้วเพิ่มทีละตัวตามนี้:

   | Name (พิมพ์ให้ตรง) | Value (ค่าจริงของคุณ) | ใช้กับ |
   |--------------------|------------------------|--------|
   | `RESEND_API_KEY` | ค่า API Key จาก Resend | อีเมล |
   | `FROM_EMAIL` | อีเมลผู้ส่ง เช่น `onboarding@resend.dev` | อีเมล |
   | `LINE_CHANNEL_ACCESS_TOKEN` | Channel Access Token จาก LINE | Line |
   | `LINE_GROUP_ID` | Group ID (ขึ้นต้นด้วยตัว C) | Line |

4. เลือก **Environment**: อย่างน้อยเลือก **Production** (ถ้าอยากให้ Preview ใช้ด้วย ให้เลือกครบ)

5. กด **Save** แล้ว **Redeploy** โปรเจกต์หนึ่งครั้ง (Deployments → ... → Redeploy)

หลัง redeploy แล้ว ปุ่มส่ง Line และอีเมลบนเว็บ Vercel จะใช้ค่านี้ส่งได้

---

## 2️⃣ ตั้งค่าบนเครื่องตัวเอง (Local – ให้ส่ง Line & Email ตอนรันในเครื่อง)

ตอนรัน `npm run dev` ตัวแอปจะยิงไปที่ `/api/send-email` และ `/api/send-line`  
ถ้าอยากให้ส่งจริงบนเครื่องตัวเอง มี 2 แบบ:

### วิธี A: ใช้ `vercel dev` (แนะนำ – ใช้ชุด API เดียวกับบน Vercel)

1. ติดตั้ง Vercel CLI (ครั้งเดียว):
   ```bash
   npm i -g vercel
   ```

2. ในโฟลเดอร์โปรเจกต์ สร้างไฟล์ **`.env.local`** (อยู่ระดับเดียวกับ `package.json`):

   - คัดลอกจาก `.env.example` มาวาง
   - เปลี่ยนค่าตรง `xxxx...` เป็นค่าจริงของคุณ (ใช้ชุดเดียวกับที่ใส่ใน Vercel ก็ได้)

   ตัวอย่างเนื้อหา `.env.local`:
   ```
   RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
   FROM_EMAIL=onboarding@resend.dev
   LINE_CHANNEL_ACCESS_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxx
   LINE_GROUP_ID=Cxxxxxxxxxxxxxxxxxxxxxxxx
   ```

3. รันแอปด้วยคำสั่ง:
   ```bash
   vercel dev
   ```
   แทน `npm run dev`  
   แล้วเปิดในเบราว์เซอร์ตามที่มันบอก (เช่น `http://localhost:3000`)  
   ตอนกดส่ง Line / อีเมล จะใช้ค่าจาก `.env.local` ส่งจริง

### วิธี B: ใช้แค่ `npm run dev`

ถ้ารันแค่ `npm run dev` (Vite) จะ**ไม่มี** route `/api/...` ในเครื่อง  
ดังนั้นปุ่มส่ง Line/Email จะเรียกแล้ว error (เช่น 404)  
ถ้าอยากให้ส่งได้บน local ต้องใช้ **วิธี A** (`vercel dev` + `.env.local`)

---

## 3️⃣ หาค่าแต่ละตัวมาจากไหน (แบบย่อ)

- **RESEND_API_KEY และ FROM_EMAIL**  
  - ไปที่ [resend.com](https://resend.com) → สมัคร/เข้าสู่ระบบ  
  - ไปที่ API Keys → Create API Key → copy มาใส่ใน `RESEND_API_KEY`  
  - `FROM_EMAIL` ใช้ได้ `onboarding@resend.dev` (ทดสอบ) หรืออีเมลที่ verify แล้ว

- **LINE_CHANNEL_ACCESS_TOKEN และ LINE_GROUP_ID**  
  - ไปที่ [LINE Developers](https://developers.line.biz) → สร้าง Channel (Messaging API)  
  - ใน Channel → tab "Messaging API" → อ่าน **Channel access token** → ใส่ใน `LINE_CHANNEL_ACCESS_TOKEN`  
  - เพิ่ม Bot เข้ากลุ่ม Line → ในกลุ่มส่งข้อความ `/getid` (หรือใช้วิธีอื่นตามที่ LINE แนะนำ) → ได้ **Group ID** (ขึ้นต้นด้วย C) → ใส่ใน `LINE_GROUP_ID`

---

## 4️⃣ เช็คว่าทำถูกไหม

- **บน Vercel**  
  เปิดแอปที่ลิงก์ Vercel → ไปหน้า ตั้งค่า → กดปุ่มทดสอบส่ง Line / ส่งอีเมล  
  - ส่งได้ = ตั้งค่า Environment Variables ถูก และ redeploy แล้ว  
  - ส่งไม่ได้ = ตรวจชื่อตัวแปร (ต้องตรงกับตารางด้านบน) และลอง redeploy อีกครั้ง

- **บนเครื่อง (vercel dev)**  
  รัน `vercel dev` → เปิดแอป → กดปุ่มทดสอบส่ง  
  - ส่งได้ = มีไฟล์ `.env.local` และค่าถูกต้อง  
  - ส่งไม่ได้ = ตรวจว่า `.env.local` อยู่ที่ root โปรเจกต์ และค่าไม่มีการเว้นวรรค/อัญประกาศเกิน

---

## ⚠️ สิ่งที่ไม่ควรทำ

- **อย่า** ใส่ `RESEND_API_KEY` หรือ `LINE_CHANNEL_ACCESS_TOKEN` ลงในไฟล์ `.ts` / `.tsx` แล้ว commit ขึ้น Git  
- **อย่า** commit ไฟล์ `.env` หรือ `.env.local` (ในโปรเจกต์นี้มี `.gitignore` กันไว้แล้ว)  
- เก็บคีย์และ Token เฉพาะใน **Vercel Environment Variables** และใน **.env.local** (เฉพาะเครื่องตัวเอง)

ถ้าทำตามนี้ ทั้ง **รันบนเครื่อง (vercel dev)** และ **เปิดบน Vercel** จะส่ง Line และอีเมลได้แบบปลอดภัย
